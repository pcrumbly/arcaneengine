import { evaluateCondition, resolveCharacterEffects } from './rules.ts';
async function present(base44:any,session:any,character:any){const [graph,instance]=await Promise.all([base44.asServiceRole.entities.DialogueGraph.get(session.dialogue_graph_id),base44.asServiceRole.entities.NPCInstance.get(session.npc_instance_id)]);const definition=await base44.asServiceRole.entities.NPCDefinition.get(instance.definition_id);const node=(graph.nodes||[]).find((item:any)=>item.key===session.current_node_key)||null;const options:any[]=[];for(const option of node?.options||[])if(await evaluateCondition(base44,{character},option.conditions))options.push({key:option.key,label:option.label});return {session,npc:{instance,definition},graph:{id:graph.id,name:graph.name},node:node?{key:node.key,text:node.text,type:node.type||'text'}:null,options};}
export async function loadVisibleNpcs(base44:any,character:any){const placements=await base44.asServiceRole.entities.NPCPlacement.filter({game_id:character.game_id,content_version:character.content_version,location_id:character.current_location_id,enabled:true},'created_date',100);const visible:any[]=[];for(const placement of placements){if(!await evaluateCondition(base44,{character},placement.visibility_conditions))continue;const instance=await base44.asServiceRole.entities.NPCInstance.get(placement.npc_instance_id);if(instance.availability!=='available')continue;const definition=await base44.asServiceRole.entities.NPCDefinition.get(instance.definition_id);if(!await evaluateCondition(base44,{character},definition.visibility_conditions))continue;visible.push({placement,instance,definition,can_interact:await evaluateCondition(base44,{character},placement.interaction_conditions)});}return visible;}
async function updateRelationship(base44:any,character:any,npcInstanceId:string,effects:any[]){for(const effect of effects.filter((item:any)=>item.type==='relationshipChange')){const rows=await base44.asServiceRole.entities.Relationship.filter({character_id:character.id,npc_instance_id:npcInstanceId},'-created_date',1);const current=rows[0],values={...(current?.values||{})},key=effect.relationshipKey||'affinity';values[key]=(values[key]||0)+Number(effect.amount||0);if(current)await base44.asServiceRole.entities.Relationship.update(current.id,{values,version:current.version+1});else await base44.asServiceRole.entities.Relationship.create({game_id:character.game_id,content_version:character.content_version,character_id:character.id,npc_instance_id:npcInstanceId,values,tags:[],version:1});}}
export async function handleDialogueCommand(base44:any,user:any,body:any,requestId:string){if(body.command==='START_DIALOGUE'){const character=await base44.entities.Character.get(body.characterId);const placement=await base44.asServiceRole.entities.NPCPlacement.get(body.placementId);if(placement.location_id!==character.current_location_id||placement.game_id!==character.game_id||placement.content_version!==character.content_version||!placement.enabled)return Response.json({error:'This NPC is not available at the current location.'},{status:422});const instance=await base44.asServiceRole.entities.NPCInstance.get(placement.npc_instance_id),definition=await base44.asServiceRole.entities.NPCDefinition.get(instance.definition_id);if(instance.availability!=='available'||!(definition.actions||[]).includes('talk'))return Response.json({error:'This NPC is not available for conversation.'},{status:422});if(!await evaluateCondition(base44,{character},definition.visibility_conditions)||!await evaluateCondition(base44,{character},placement.visibility_conditions)||!await evaluateCondition(base44,{character},placement.interaction_conditions))return Response.json({error:'This character cannot begin that conversation.'},{status:422});const graphId=body.dialogueGraphId||definition.dialogue_graph_ids?.[0];if(!graphId||(definition.dialogue_graph_ids||[]).includes(graphId)===false)return Response.json({error:'No valid dialogue is configured for this NPC.'},{status:422});const graph=await base44.asServiceRole.entities.DialogueGraph.get(graphId);if(graph.game_id!==character.game_id||graph.content_version!==character.content_version)return Response.json({error:'Dialogue content is not valid for this character.'},{status:422});const session=await base44.asServiceRole.entities.DialogueSession.create({game_id:character.game_id,content_version:character.content_version,character_id:character.id,npc_instance_id:instance.id,dialogue_graph_id:graph.id,current_node_key:graph.start_node_key,state:'ACTIVE',variables:{},version:1});await base44.asServiceRole.entities.DomainEvent.create({game_id:character.game_id,character_id:character.id,event_type:'npc.interaction.started',aggregate_type:'dialogue',aggregate_id:session.id,request_id:requestId,content_version:character.content_version,payload:{npc_definition_id:definition.id},occurred_at:new Date().toISOString()});await base44.asServiceRole.entities.AuditEvent.create({game_id:character.game_id,actor_user_id:user.id,character_id:character.id,command:body.command,request_id:requestId,result:'accepted',details:{dialogue_session_id:session.id,npc_instance_id:instance.id},occurred_at:new Date().toISOString()});return Response.json({dialogue:await present(base44,session,character)});}
const original=await base44.asServiceRole.entities.DialogueSession.get(body.sessionId);const character=await base44.entities.Character.get(original.character_id);const duplicate=await base44.asServiceRole.entities.AuditEvent.filter({actor_user_id:user.id,request_id:requestId,result:'accepted'},'-occurred_at',1);if(duplicate.length){const latest=await base44.asServiceRole.entities.DialogueSession.get(original.id);return Response.json({dialogue:await present(base44,latest,character)});}if(original.state!=='ACTIVE'||original.version!==body.sessionVersion)return Response.json({error:'Dialogue state changed. Refresh and try again.'},{status:409});const graph=await base44.asServiceRole.entities.DialogueGraph.get(original.dialogue_graph_id),node=(graph.nodes||[]).find((item:any)=>item.key===original.current_node_key),option=(node?.options||[]).find((item:any)=>item.key===body.optionKey);if(!option||!await evaluateCondition(base44,{character},option.conditions))return Response.json({error:'That dialogue option is not available.'},{status:422});const effects=option.effects||[],characterEffects=effects.filter((item:any)=>item.type!=='relationshipChange');let currentCharacter=character;if(characterEffects.length){const resolved=await resolveCharacterEffects(base44,character,characterEffects);currentCharacter=await base44.entities.Character.update(character.id,{...resolved.patch,version:character.version+1});}await updateRelationship(base44,currentCharacter,original.npc_instance_id,effects);const completed=!option.nextNodeKey,nextKey=option.nextNodeKey||original.current_node_key;const session=await base44.asServiceRole.entities.DialogueSession.update(original.id,{current_node_key:nextKey,state:completed?'COMPLETED':'ACTIVE',completed_at:completed?new Date().toISOString():undefined,version:original.version+1});await base44.asServiceRole.entities.DomainEvent.create({game_id:character.game_id,character_id:character.id,event_type:'dialogue.option.selected',aggregate_type:'dialogue',aggregate_id:session.id,request_id:requestId,content_version:character.content_version,payload:{node_key:node.key,option_key:option.key,completed},occurred_at:new Date().toISOString()});await base44.asServiceRole.entities.AuditEvent.create({game_id:character.game_id,actor_user_id:user.id,character_id:character.id,command:body.command,request_id:requestId,result:'accepted',details:{dialogue_session_id:session.id,node_key:node.key,option_key:option.key},occurred_at:new Date().toISOString()});return Response.json({dialogue:await present(base44,session,currentCharacter)});}