import { RefreshCw } from 'lucide-react';
const filters=[['all','All'],['active','In progress'],['complete','Completed'],['claimable','Ready to claim'],['claimed','Claimed']];
export default function AchievementControls({filter,onFilterChange,characters,characterId,onCharacterChange,onRefresh,busy}){
 return <div className="flex flex-col gap-3 lg:flex-row lg:items-center lg:justify-between"><div role="tablist" aria-label="Achievement views" className="flex gap-1 overflow-x-auto rounded-md border border-white/10 bg-runtime-surface p-1">{filters.map(([value,label])=><button key={value} role="tab" aria-selected={filter===value} disabled={busy} onClick={()=>onFilterChange(value)} className={`whitespace-nowrap rounded px-3 py-1.5 text-xs disabled:opacity-50 ${filter===value?'bg-runtime-accent text-slate-950':'text-slate-400'}`}>{label}</button>)}</div><div className="flex gap-2"><select aria-label="Achievement operator" value={characterId} disabled={busy} onChange={event=>onCharacterChange(event.target.value)} className="min-w-0 flex-1 rounded border border-white/10 bg-runtime px-3 py-2 text-sm text-runtime-text sm:w-48">{characters.map(character=><option key={character.id} value={character.id}>{character.name}</option>)}</select><button type="button" disabled={busy} onClick={onRefresh} className="flex items-center gap-2 rounded border border-white/10 px-3 py-2 text-sm disabled:opacity-50"><RefreshCw size={15} className={busy?'animate-spin':''}/>Refresh</button></div></div>;
}