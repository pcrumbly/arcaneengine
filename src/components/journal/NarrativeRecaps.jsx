export default function NarrativeRecaps({recaps=[],facts=[],relationships=[]}){return <section className="rounded-lg border border-white/10 bg-runtime-surface p-4"><h2 className="font-semibold">Narrative recap <span className="text-xs font-normal text-slate-500">({recaps.length})</span></h2><div className="mt-3 space-y-3">{recaps.map(recap=><article key={recap.id} className="rounded border border-white/10 p-3"><div className="flex items-start justify-between gap-3"><h3 className="text-sm font-medium">{recap.title}</h3><span className="text-[11px] capitalize text-slate-500">{recap.recap_type.replaceAll('_',' ')}</span></div><p className="mt-1 text-sm text-slate-400">{recap.summary}</p><p className="mt-2 text-xs text-slate-600">World minute {recap.world_minute}</p></article>)}{!recaps.length&&<p className="text-sm text-slate-500">No narrative outcomes recorded yet.</p>}</div>{!!facts.length&&<div className="mt-4 border-t border-white/10 pt-3"><h3 className="text-xs uppercase tracking-wider text-slate-500">Established facts</h3><div className="mt-2 flex flex-wrap gap-2">{facts.map(fact=><span key={fact.id} className="rounded bg-white/5 px-2 py-1 text-xs text-slate-300">{fact.fact_key}: {typeof fact.value==='object'?JSON.stringify(fact.value):String(fact.value)}</span>)}</div></div>}{!!relationships.length&&<div className="mt-4 border-t border-white/10 pt-3"><h3 className="text-xs uppercase tracking-wider text-slate-500">Quest relationships</h3><div className="mt-2 space-y-1">{relationships.map(row=><p key={row.id} className="text-xs text-slate-300"><span className="capitalize">{row.source_quest_key}</span> → <span className="capitalize">{row.target_quest_key}</span>: {row.relationship_key} {row.value}</p>)}</div></div>}</section>}