import { RefreshCw,Search } from 'lucide-react';
export default function JournalControls({characters,characterId,onCharacterChange,query,onQueryChange,questState,onQuestStateChange,eventCategory,onEventCategoryChange,categories,onRefresh,busy}){
 const input='rounded border border-white/10 bg-runtime px-3 py-2 text-sm text-runtime-text disabled:opacity-50';
 return <div className="grid gap-2 rounded-lg border border-white/10 bg-runtime-surface p-3 md:grid-cols-2 xl:grid-cols-[1fr_1fr_160px_160px_auto]"><select aria-label="Journal operator" value={characterId} disabled={busy} onChange={event=>onCharacterChange(event.target.value)} className={input}>{characters.map(character=><option key={character.id} value={character.id}>{character.name}</option>)}</select><label className="relative"><span className="sr-only">Search journal</span><Search size={15} className="absolute left-3 top-2.5 text-slate-500"/><input value={query} onChange={event=>onQueryChange(event.target.value)} placeholder="Search journal" className={`${input} w-full pl-9`}/></label><select aria-label="Quest status" value={questState} onChange={event=>onQuestStateChange(event.target.value)} className={input}><option value="all">All quest states</option>{['ACTIVE','COMPLETED','FAILED','ABANDONED'].map(state=><option key={state} value={state}>{state.toLowerCase()}</option>)}</select><select aria-label="Event category" value={eventCategory} onChange={event=>onEventCategoryChange(event.target.value)} className={input}><option value="all">All event types</option>{categories.map(category=><option key={category} value={category}>{category}</option>)}</select><button type="button" disabled={busy} onClick={onRefresh} className={`${input} flex items-center justify-center gap-2`}><RefreshCw size={15} className={busy?'animate-spin':''}/>Refresh</button></div>;
}