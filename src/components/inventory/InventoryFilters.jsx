export default function InventoryFilters({categories,category,onCategoryChange,containers,containerId,onContainerChange,qualities,quality,onQualityChange,assignment,onAssignmentChange}){
  return <div className="mt-5 border-b border-white/10 pb-3"><div role="tablist" aria-label="Item categories" className="flex gap-1 overflow-x-auto"><Tab active={category==='all'} onClick={()=>onCategoryChange('all')}>All</Tab>{categories.map(value=><Tab key={value} active={category===value} onClick={()=>onCategoryChange(value)}>{value}</Tab>)}</div><div className="mt-3 flex flex-wrap gap-3"><Filter label="Container" value={containerId} onChange={onContainerChange}><option value="all">All containers</option>{containers.map(container=><option key={container.id} value={container.id}>{container.name} · {container.owner_type==='party'?'Shared':'Member'}</option>)}</Filter><Filter label="Quality" value={quality} onChange={onQualityChange}><option value="all">All qualities</option>{qualities.map(value=><option key={value} value={value}>{value}</option>)}</Filter><Filter label="Assignment" value={assignment} onChange={onAssignmentChange}><option value="all">All items</option><option value="equipped">Equipped</option><option value="carried">Not equipped</option></Filter></div></div>;
}

function Filter({label,value,onChange,children}){return <label className="flex items-center gap-2 text-xs text-slate-400"><span>{label}</span><select value={value} onChange={event=>onChange(event.target.value)} className="rounded-md border border-white/10 bg-runtime-surface px-3 py-2 text-sm capitalize text-slate-100">{children}</select></label>;}

function Tab({active,onClick,children}){
  return <button role="tab" aria-selected={active} onClick={onClick} className={`whitespace-nowrap border-b-2 px-3 py-2 text-sm capitalize ${active?'border-cyan-400 text-cyan-200':'border-transparent text-slate-400 hover:text-slate-200'}`}>{children}</button>;
}