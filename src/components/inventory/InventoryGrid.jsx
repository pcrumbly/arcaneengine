import { PackageOpen } from 'lucide-react';
import ItemActionMenu from '@/components/inventory/ItemActionMenu';

export default function InventoryGrid({items,selectedIds,onToggle,onInspect,onAction,busy}){
  if(!items.length)return <div className="grid place-items-center gap-2 rounded-lg border border-white/10 bg-runtime-surface p-10 text-sm text-slate-500"><PackageOpen size={22}/>No matching items</div>;
  return <div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-3">{items.map(item=><article key={item.id} className="rounded-lg border border-white/10 bg-runtime-surface p-4"><div className="flex items-start justify-between gap-3"><div className="flex gap-3"><input aria-label={`Select ${item.definition.name}`} type="checkbox" disabled={!!item.equipped_slot} checked={selectedIds.includes(item.id)} onChange={()=>onToggle(item.id)} className="mt-1 accent-cyan-400 disabled:opacity-30"/><div><h3 className="font-medium">{item.definition.name}</h3><p className="mt-1 text-xs capitalize text-slate-500">{item.definition.category} · {item.quality}</p></div></div><span className="rounded bg-white/5 px-2 py-1 text-xs">×{item.quantity}</span></div><p className="mt-4 text-xs text-slate-400">{item.container?.name||'Unknown container'}{item.container?.container_type==='loot'&&<span className="ml-2 text-amber-200">Loot</span>}</p><div className="mt-4 flex items-center justify-between"><span className="text-xs text-slate-500">Weight {(item.definition.weight||0)*item.quantity}</span><ItemActionMenu item={item} busy={busy} onInspect={onInspect} onAction={onAction}/></div></article>)}</div>;
}