export default function SoundPanel({settings,onChange,onSave,busy}) { const update=(key,value)=>onChange({...settings,[key]:value}); return <section className="max-w-2xl space-y-5 rounded-lg border border-white/10 bg-[#0a1728] p-5"><h3 className="font-semibold">Sound channels</h3>{['master','interface','music','effects','notifications'].map(key=><label key={key} className="grid grid-cols-[110px_1fr_42px] items-center gap-3 text-sm capitalize"><span>{key}</span><input aria-label={`${key} volume`} type="range" min="0" max="100" value={settings[key]} onChange={e=>update(key,Number(e.target.value))} className="accent-cyan-400"/><output>{settings[key]}%</output></label>)}<label className="flex items-center gap-3 text-sm"><input type="checkbox" checked={settings.mute_inactive} onChange={e=>update('mute_inactive',e.target.checked)} className="size-4 accent-cyan-400"/>Mute when application is inactive</label><button disabled={busy} onClick={onSave} className="rounded bg-cyan-400 px-3 py-2 text-sm font-semibold text-slate-950 disabled:opacity-50">Save sound settings</button></section>; }