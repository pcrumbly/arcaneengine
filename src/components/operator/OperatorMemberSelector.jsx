import { Crown, UserRound } from 'lucide-react';
export default function OperatorMemberSelector({members,selectedId,onSelect}){return <section className="rounded-lg border border-white/10 bg-runtime-surface p-3"><div className="mb-3 flex items-center justify-between"><div><p className="text-xs uppercase tracking-wider text-runtime-accent">Active operators</p><h2 className="mt-1 font-semibold">Party members</h2></div><span className="text-xs text-slate-500">{members.length} member{members.length===1?'':'s'}</span></div><div className="flex gap-2 overflow-x-auto pb-1">{members.map(member=>{const character=member.character||member,active=character.id===selectedId;return <button key={character.id} onClick={()=>onSelect(character.id)} aria-pressed={active} className={`min-w-44 rounded-md border p-3 text-left transition-colors ${active?'border-runtime-accent bg-runtime-accent/10':'border-white/10 hover:border-white/20'}`}><span className="flex items-center gap-2 text-sm font-medium">{member.role==='leader'?<Crown size={14} className="text-amber-300"/>:<UserRound size={14} className="text-slate-500"/>}{character.name}</span><span className="mt-1 block text-xs capitalize text-slate-500">{member.role||'operator'}{member.formation_position?` · ${member.formation_position}`:''}</span></button>})}</div></section>;}