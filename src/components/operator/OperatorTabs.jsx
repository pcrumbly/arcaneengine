import { PackageSearch, UserRound, Users } from 'lucide-react';
const tabs=[{id:'profile',label:'Profile',icon:UserRound},{id:'inventory',label:'Inventory',icon:PackageSearch},{id:'party',label:'Party',icon:Users}];
export default function OperatorTabs({active,onChange}){return <div role="tablist" aria-label="Operator sections" className="flex gap-1 overflow-x-auto border-b border-white/10">{tabs.map(({id,label,icon:Icon})=><button key={id} role="tab" aria-selected={active===id} onClick={()=>onChange(id)} className={`flex items-center gap-2 whitespace-nowrap border-b-2 px-4 py-3 text-sm ${active===id?'border-runtime-accent text-runtime-accent':'border-transparent text-slate-400 hover:text-runtime-text'}`}><Icon size={16}/>{label}</button>)}</div>;}