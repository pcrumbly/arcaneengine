import { CheckCircle2,XCircle } from 'lucide-react';
export default function InteractionResult({result,trace}){if(!result)return null;const success=result.status==='success',Icon=success?CheckCircle2:XCircle;return <div className={`mb-3 rounded-md border p-3 text-sm ${success?'border-emerald-400/25 bg-emerald-400/5':'border-amber-400/25 bg-amber-400/5'}`}><div className="flex items-center gap-2 font-semibold"><Icon size={15} className={success?'text-emerald-300':'text-amber-300'}/>{result.action?.label}: {result.status}</div>{result.check&&<p className="mt-1 text-xs text-slate-400">{result.check.label}: {result.check.roll} + {result.check.attribute} + {result.check.skill_rank} = {result.check.total} vs {result.check.difficulty}</p>}<details className="mt-2 text-xs text-slate-500"><summary className="cursor-pointer">Interaction trace</summary><ol className="mt-2 space-y-1">{(trace?.steps||[]).map((step,index)=><li key={index}>{index+1}. {String(step.step).replaceAll('_',' ')}</li>)}</ol></details></div>}