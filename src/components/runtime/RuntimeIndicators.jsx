import { Activity,Coins,MapPin,UserRound } from 'lucide-react';
import { term } from '@/lib/runtimeManifest';
const legacy={character:{type:'character'},location:{type:'location'},resource:{type:'resource',aggregate:'first'},currency:{type:'currency',aggregate:'sum'}};
export default function RuntimeIndicators({state,game}){const character=state?.character,translations=state?.translations||{},configured=game?.header_indicators||Object.keys(legacy),indicators=configured.map(item=>typeof item==='string'?{...legacy[item],id:item}:item).filter(Boolean);const label=item=>translations[item.label_key]||item.label;return <div className="hidden items-center gap-4 text-xs text-slate-400 lg:flex">{indicators.map((item,index)=>{let value=null,Icon=Activity;if(item.type==='character'&&character){value=character.name;Icon=UserRound}else if(item.type==='location'&&state?.location){value=state.location.name;Icon=MapPin}else if(item.type==='resource'&&character){const entry=item.key?[item.key,character.resources?.[item.key]]:Object.entries(character.resources||{})[0];if(entry&&entry[1]!==undefined)value=`${label(item)||entry[0]} ${entry[1]}`;Icon=Activity}else if(item.type==='currency'&&character){const entry=item.key?[item.key,character.currency?.[item.key]]:null;if(entry&&entry[1]!==undefined)value=`${label(item)||entry[0]} ${entry[1]}`;else if(item.aggregate==='sum')value=Object.values(character.currency||{}).reduce((sum,current)=>sum+Number(current),0);Icon=Coins}else if(item.type==='attribute'&&character&&item.key)value=`${label(item)||item.key} ${character.attributes?.[item.key]??0}`;else if(item.type==='text')value=translations[item.value_key]||item.value;if(value===null)return null;return <span key={item.id||`${item.type}-${index}`} className="flex items-center gap-1"><Icon size={14}/>{item.type==='character'&&label(item)!==''?`${label(item)||term(game,'character','Character',translations)}: `:''}<b className="font-medium text-runtime-text">{value}</b></span>})}</div>}