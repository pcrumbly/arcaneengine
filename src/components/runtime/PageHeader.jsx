import { useRuntimeText } from '@/lib/RuntimeI18nContext';
const key=value=>String(value||'').toLowerCase().replace(/[^a-z0-9]+/g,'_').replace(/^_|_$/g,'');
export default function PageHeader({eyebrow,title,description,actions}){
  const text=useRuntimeText(),pageKey=key(title),localizedTitle=text(`page.${pageKey}.title`,title),localizedDescription=text(`page.${pageKey}.description`,description),crumbs=eyebrow.split('/').map(value=>text(`label.${key(value)}`,value.trim()));
  return <header className="flex flex-col gap-4 border-b border-white/10 pb-5 sm:flex-row sm:items-end sm:justify-between"><div><nav aria-label="Breadcrumb" className="flex items-center gap-2 text-xs uppercase tracking-[.18em] text-slate-500">{crumbs.map((crumb,index)=><span key={crumb} className="flex items-center gap-2">{index>0&&<span aria-hidden="true">/</span>}<span className={index===crumbs.length-1?'text-runtime-accent':''}>{crumb}</span></span>)}</nav><h1 className="mt-2 text-2xl font-semibold tracking-tight sm:text-3xl">{localizedTitle}</h1>{localizedDescription&&<p className="mt-1 max-w-2xl text-sm text-slate-400">{localizedDescription}</p>}</div>{actions&&<div className="flex shrink-0 flex-wrap gap-2">{actions}</div>}</header>;
}