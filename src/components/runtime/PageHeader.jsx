export default function PageHeader({eyebrow,title,description,actions}){
  const crumbs=eyebrow.split('/').map(value=>value.trim());
  return <header className="flex flex-col gap-4 border-b border-white/10 pb-5 sm:flex-row sm:items-end sm:justify-between"><div><nav aria-label="Breadcrumb" className="flex items-center gap-2 text-xs uppercase tracking-[.18em] text-slate-500">{crumbs.map((crumb,index)=><span key={crumb} className="flex items-center gap-2">{index>0&&<span aria-hidden="true">/</span>}<span className={index===crumbs.length-1?'text-runtime-accent':''}>{crumb}</span></span>)}</nav><h1 className="mt-2 text-2xl font-semibold tracking-tight sm:text-3xl">{title}</h1>{description&&<p className="mt-1 max-w-2xl text-sm text-slate-400">{description}</p>}</div>{actions&&<div className="flex shrink-0 flex-wrap gap-2">{actions}</div>}</header>;
}