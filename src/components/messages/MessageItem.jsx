import { ChevronRight, Mail, MailOpen } from 'lucide-react';
export default function MessageItem({item,busy,onOpen}){const Icon=item.read?MailOpen:Mail,date=item.created_date?new Date(item.created_date).toLocaleString():'';return <button type="button" disabled={busy} onClick={()=>onOpen(item)} className={`flex w-full items-start gap-3 p-4 text-left transition-colors hover:bg-white/5 disabled:opacity-60 ${item.read?'text-slate-500':'text-runtime-text'}`}><Icon size={17} className={`mt-0.5 shrink-0 ${item.read?'text-slate-600':'text-runtime-accent'}`}/><span className="min-w-0 flex-1"><span className="flex flex-wrap items-center gap-2"><strong className="text-sm">{item.title}</strong>{!item.read&&<span className="rounded-full bg-runtime-accent/15 px-2 py-0.5 text-[10px] font-semibold uppercase text-runtime-accent">Unread</span>}</span><span className="mt-1 block whitespace-pre-wrap text-sm leading-6 text-slate-400">{item.message}</span><span className="mt-2 block text-[11px] capitalize text-slate-600">{item.category||'system'}{date&&` · ${date}`}</span></span>{item.link&&<ChevronRight size={16} className="mt-1 shrink-0 text-slate-600"/>}</button>}