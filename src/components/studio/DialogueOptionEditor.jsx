import { Trash2 } from 'lucide-react';
import JsonValueField from '@/components/studio/JsonValueField';

export default function DialogueOptionEditor({ option, nodeKeys, onChange, onRemove }) {
  const set = (key, value) => onChange({ ...option, [key]: value });
  const eventTypes = (option.events || []).map(event => event.event_type).filter(Boolean);
  return <div className="rounded border border-white/10 bg-runtime-surface p-3"><div className="grid gap-2 sm:grid-cols-2"><input value={option.key || ''} onChange={event => set('key', event.target.value)} placeholder="Stable choice key" className="rounded border border-white/10 bg-runtime px-3 py-2 text-sm"/><input value={option.label || ''} onChange={event => set('label', event.target.value)} placeholder="Player-facing choice" className="rounded border border-white/10 bg-runtime px-3 py-2 text-sm"/><select value={option.nextNodeKey || ''} onChange={event => set('nextNodeKey', event.target.value)} className="rounded border border-white/10 bg-runtime px-3 py-2 text-sm"><option value="">End conversation</option>{nodeKeys.map(key => <option key={key} value={key}>Continue to: {key}</option>)}</select><input value={eventTypes.join(', ')} onChange={event => set('events', event.target.value.split(',').map(value => value.trim()).filter(Boolean).map(event_type => ({ event_type })))} placeholder="Quest events, comma-separated" className="rounded border border-white/10 bg-runtime px-3 py-2 text-sm"/></div><div className="mt-2 grid gap-2 sm:grid-cols-2"><JsonValueField label="Availability condition" value={option.conditions} emptyValue={{}} onChange={value => set('conditions', value)}/><JsonValueField label="Character and relationship effects" value={option.effects} emptyValue={[]} onChange={value => set('effects', value)}/></div><div className="mt-2 flex justify-end"><button type="button" onClick={onRemove} aria-label="Remove dialogue choice" className="text-red-300"><Trash2 size={15}/></button></div></div>;
}