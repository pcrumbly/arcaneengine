import { useEffect,useState } from 'react';
import { Activity,Archive,FileClock,ShieldAlert } from 'lucide-react';
import { invokeRuntimeCommand } from '@/lib/runtimeCommandClient';
const cards=[['commands','Commands',Activity],['warnings','Warnings',ShieldAlert],['snapshots','Snapshots',Archive],['migrations','Migrations',FileClock]];
export default function OperationsPanel(){const [data,setData]=useState(null);useEffect(()=>{invokeRuntimeCommand({command:'GET_OPERATIONS'}).then(({data})=>setData(data))},[]);if(!data)return <div className="rounded-lg border border-white/10 p-4 text-sm text-slate-500">Loading operations…</div>;return <section className="rounded-lg border border-white/10 bg-[#0a1728]"><div className="border-b border-white/10 px-4 py-3"><h3 className="font-semibold">Operations</h3><p className="text-xs text-slate-500">Recent command volume, throttling, and recovery records.</p></div><div className="grid grid-cols-2 gap-px bg-white/5 sm:grid-cols-4">{cards.map(([key,label,Icon])=><div key={key} className="bg-[#0a1728] p-4"><Icon size={16} className="text-cyan-400"/><p className="mt-2 text-2xl font-semibold">{data.counts[key]}</p><p className="text-xs text-slate-500">{label}</p></div>)}</div><div className="divide-y divide-white/5 border-t border-white/10">{data.events.length?data.events.map(event=><div key={event.id} className="grid gap-1 px-4 py-3 text-sm sm:grid-cols-[100px_1fr_auto]"><span className={event.severity==='critical'?'text-red-300':'text-amber-300'}>{event.severity}</span><span>{event.message}</span><span className="text-xs text-slate-600">{event.command}</span></div>):<p className="p-4 text-sm text-slate-500">No operational warnings recorded.</p>}</div></section>}