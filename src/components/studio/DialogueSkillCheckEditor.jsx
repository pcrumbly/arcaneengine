import JsonValueField from '@/components/studio/JsonValueField';

export default function DialogueSkillCheckEditor({ value, nodeKeys, onChange }) {
  const check = value || {}, set = (key, next) => onChange({ ...check, [key]: next });
  const setEvents = (key, text) => set(key, text.split(',').map(item => item.trim()).filter(Boolean).map(event_type => ({ event_type })));
  return <div className="mt-2 rounded border border-runtime-accent/20 bg-runtime-accent/5 p-3"><div className="mb-2 flex items-center justify-between"><p className="text-xs font-semibold uppercase tracking-wider text-runtime-accent">Deterministic skill check</p><button type="button" onClick={() => onChange(null)} className="text-xs text-slate-400">Remove</button></div><div className="grid gap-2 sm:grid-cols-2"><input value={check.attribute_id || ''} onChange={event => set('attribute_id', event.target.value)} placeholder="Attribute key" className="rounded border border-white/10 bg-runtime px-3 py-2 text-sm"/><input type="number" min="1" value={check.difficulty || 10} onChange={event => set('difficulty', Number(event.target.value) || 1)} aria-label="Check difficulty" className="rounded border border-white/10 bg-runtime px-3 py-2 text-sm"/><input type="number" min="2" value={check.die_sides || 20} onChange={event => set('die_sides', Number(event.target.value) || 20)} aria-label="Die sides" className="rounded border border-white/10 bg-runtime px-3 py-2 text-sm"/><select value={check.success_node_key || ''} onChange={event => set('success_node_key', event.target.value)} className="rounded border border-white/10 bg-runtime px-3 py-2 text-sm"><option value="">Success ends conversation</option>{nodeKeys.map(key => <option key={key} value={key}>Success: {key}</option>)}</select><select value={check.failure_node_key || ''} onChange={event => set('failure_node_key', event.target.value)} className="rounded border border-white/10 bg-runtime px-3 py-2 text-sm"><option value="">Failure ends conversation</option>{nodeKeys.map(key => <option key={key} value={key}>Failure: {key}</option>)}</select><input value={(check.success_events || []).map(item => item.event_type).filter(Boolean).join(', ')} onChange={event => setEvents('success_events', event.target.value)} placeholder="Success quest events" className="rounded border border-white/10 bg-runtime px-3 py-2 text-sm"/><input value={(check.failure_events || []).map(item => item.event_type).filter(Boolean).join(', ')} onChange={event => setEvents('failure_events', event.target.value)} placeholder="Failure quest events" className="rounded border border-white/10 bg-runtime px-3 py-2 text-sm"/></div><div className="mt-2 grid gap-2 sm:grid-cols-2"><JsonValueField label="Success effects" value={check.success_effects} emptyValue={[]} onChange={next => set('success_effects', next)}/><JsonValueField label="Failure effects" value={check.failure_effects} emptyValue={[]} onChange={next => set('failure_effects', next)}/></div></div>;
}