const csv=value=>String(value||'').split(',').map(item=>item.trim()).filter(Boolean);
export default function InteractionSourceEditor({value={},onChange}){const update=patch=>onChange({...value,...patch});return <div className="space-y-3"><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={!!value.required} onChange={e=>update({required:e.target.checked})}/>Require a source item</label><label className="block text-sm">Required item tags<input value={(value.item_tags||[]).join(', ')} onChange={e=>update({item_tags:csv(e.target.value)})} placeholder="tool:repair, material:metal" className="mt-1 w-full rounded border border-white/10 bg-runtime px-3 py-2"/></label><label className="block text-sm">Allowed item definition IDs<input value={(value.item_definition_ids||[]).join(', ')} onChange={e=>update({item_definition_ids:csv(e.target.value)})} className="mt-1 w-full rounded border border-white/10 bg-runtime px-3 py-2"/></label><div className="grid grid-cols-2 gap-3"><label className="text-sm">Consume item<select value={value.consume||'never'} onChange={e=>update({consume:e.target.value})} className="mt-1 w-full rounded border border-white/10 bg-runtime px-3 py-2"><option value="never">never</option><option value="always">always</option><option value="success">on success</option><option value="failure">on failure</option></select></label><label className="text-sm">Quantity<input type="number" min="1" value={value.quantity??1} onChange={e=>update({quantity:Number(e.target.value)})} className="mt-1 w-full rounded border border-white/10 bg-runtime px-3 py-2"/></label></div></div>}