import { Plus } from 'lucide-react';
export default function ContentList({label,items,selected,onSelect,onAdd,locked}){return <aside className="rounded-lg border border-white/10 bg-runtime-surface"><div className="flex items-center justify-between border-b border-white/10 p-3"><div><h3 className="font-semibold">{label}</h3><p className="text-xs text-slate-500">{items.length} records</p></div><button disabled={locked} onClick={onAdd} className="rounded bg-runtime-accent/10 p-2 text-runtime-accent disabled:opacity-30" aria-label={`Add ${label}`}><Plus size={16}/></button></div><div className="max-h-[640px] divide-y divide-white/5 overflow-y-auto">{items.map(item=><button key={item.id} onClick={()=>onSelect(item)} className={`block w-full px-3 py-3 text-left ${selected?.id===item.id?'bg-runtime-accent/10':'hover:bg-white/[.03]'}`}><strong className="block truncate text-sm">{item.name||item.label||item.key||item.id}</strong><small className="text-slate-500">{item.key||item.location_type||item.availability||'content record'}</small></button>)}{!items.length&&<p className="p-4 text-sm text-slate-500">No content in this release.</p>}</div></aside>}