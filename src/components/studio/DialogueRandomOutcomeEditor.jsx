import { Trash2 } from 'lucide-react';
import JsonValueField from '@/components/studio/JsonValueField';

export default function DialogueRandomOutcomeEditor({ outcome, nodeKeys, onChange, onRemove }) {
  const set = (key, value) => onChange({ ...outcome, [key]: value });
  return <div className="rounded border border-white/10 bg-runtime p-3"><div className="grid gap-2 sm:grid-cols-2"><input value={outcome.key || ''} onChange={event => set('key', event.target.value)} placeholder="Stable outcome key" className="rounded border border-white/10 bg-runtime-surface px-3 py-2 text-sm"/><input value={outcome.label || ''} onChange={event => set('label', event.target.value)} placeholder="Outcome label" className="rounded border border-white/10 bg-runtime-surface px-3 py-2 text-sm"/><input type="number" min="1" value={outcome.weight || 1} onChange={event => set('weight', Number(event.target.value) || 1)} aria-label="Outcome weight" className="rounded border border-white/10 bg-runtime-surface px-3 py-2 text-sm"/><select value={outcome.next_node_key || ''} onChange={event => set('next_node_key', event.target.value)} className="rounded border border-white/10 bg-runtime-surface px-3 py-2 text-sm"><option value="">End conversation</option>{nodeKeys.map(key => <option key={key} value={key}>Continue to: {key}</option>)}</select><input value={(outcome.events || []).map(item => item.event_type).filter(Boolean).join(', ')} onChange={event => set('events', event.target.value.split(',').map(item => item.trim()).filter(Boolean).map(event_type => ({ event_type })))} placeholder="Quest events, comma-separated" className="rounded border border-white/10 bg-runtime-surface px-3 py-2 text-sm sm:col-span-2"/></div><div className="mt-2"><JsonValueField label="Outcome effects" value={outcome.effects} emptyValue={[]} onChange={value => set('effects', value)}/></div><button type="button" onClick={onRemove} aria-label="Remove random outcome" className="mt-2 ml-auto block text-red-300"><Trash2 size={15}/></button></div>;
}