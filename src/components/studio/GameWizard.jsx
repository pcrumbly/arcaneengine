import { useState } from 'react';
export default function GameWizard({busy,onCreate}){const [open,setOpen]=useState(false),[values,setValues]=useState({title:'',key:'',version:'1.0.0',description:''});if(!open)return <button onClick={()=>setOpen(true)} className="rounded bg-runtime-accent px-4 py-2 text-sm font-semibold text-slate-950">Create game</button>;return <form onSubmit={e=>{e.preventDefault();onCreate(values);setOpen(false)}} className="rounded-lg border border-white/10 bg-runtime-surface p-4"><h3 className="font-semibold">New game</h3><p className="mt-1 text-xs text-slate-500">Creates the game manifest and its first draft content release.</p><div className="mt-4 grid gap-3 sm:grid-cols-3"><input required value={values.title} onChange={e=>setValues({...values,title:e.target.value})} placeholder="Game title" className="rounded border border-white/10 bg-runtime px-3 py-2 text-sm"/><input required value={values.key} onChange={e=>setValues({...values,key:e.target.value})} placeholder="Stable game key" className="rounded border border-white/10 bg-runtime px-3 py-2 text-sm"/><input required value={values.version} onChange={e=>setValues({...values,version:e.target.value})} placeholder="Initial version" className="rounded border border-white/10 bg-runtime px-3 py-2 text-sm"/></div><textarea value={values.description} onChange={e=>setValues({...values,description:e.target.value})} placeholder="Game description" className="mt-3 w-full rounded border border-white/10 bg-runtime px-3 py-2 text-sm"/><div className="mt-3 flex justify-end gap-2"><button type="button" onClick={()=>setOpen(false)} className="rounded border border-white/10 px-3 py-2 text-sm">Cancel</button><button disabled={busy} className="rounded bg-runtime-accent px-3 py-2 text-sm font-semibold text-slate-950">Create game</button></div></form>}