import { useState } from 'react';
export default function ContentPackManager({games,packs,busy,onCreate}){const [gameId,setGameId]=useState(games[0]?.id||''),[values,setValues]=useState({name:'',key:'',version:'1.0.0',packageKind:'mod',engineApiVersion:'1',dependencyPackIds:[]}),available=packs.filter(pack=>pack.game_id===gameId);const toggle=id=>setValues({...values,dependencyPackIds:values.dependencyPackIds.includes(id)?values.dependencyPackIds.filter(value=>value!==id):[...values.dependencyPackIds,id]});return <section className="rounded-lg border border-white/10 bg-runtime-surface p-4"><h2 className="font-semibold">Layered content packs</h2><p className="mt-1 text-xs text-slate-500">Dependencies load before the pack that selects them.</p><div className="mt-4 grid gap-2 sm:grid-cols-5"><select value={gameId} onChange={event=>setGameId(event.target.value)} className="rounded border border-white/10 bg-runtime px-3 py-2 text-sm">{games.map(game=><option key={game.id} value={game.id}>{game.title}</option>)}</select><input value={values.name} onChange={event=>setValues({...values,name:event.target.value})} placeholder="Pack name" className="rounded border border-white/10 bg-runtime px-3 py-2 text-sm"/><input value={values.key} onChange={event=>setValues({...values,key:event.target.value})} placeholder="Stable key" className="rounded border border-white/10 bg-runtime px-3 py-2 text-sm"/><input value={values.version} onChange={event=>setValues({...values,version:event.target.value})} placeholder="Version" className="rounded border border-white/10 bg-runtime px-3 py-2 text-sm"/><select value={values.packageKind} onChange={event=>setValues({...values,packageKind:event.target.value})} className="rounded border border-white/10 bg-runtime px-3 py-2 text-sm"><option value="mod">Mod package</option><option value="extension">Rule extension</option><option value="core">Core package</option></select></div>{available.length>0&&<div className="mt-3 flex flex-wrap gap-3">{available.map(pack=><label key={pack.id} className="flex items-center gap-2 text-xs text-slate-400"><input type="checkbox" checked={values.dependencyPackIds.includes(pack.id)} onChange={()=>toggle(pack.id)}/>{pack.name}</label>)}</div>}<button disabled={busy||!values.name.trim()||!values.key.trim()} onClick={()=>{onCreate(gameId,values);setValues({name:'',key:'',version:'1.0.0',packageKind:'mod',engineApiVersion:'1',dependencyPackIds:[]})}} className="mt-4 rounded bg-runtime-accent px-4 py-2 text-sm font-semibold text-slate-950 disabled:opacity-50">Create content pack</button></section>}