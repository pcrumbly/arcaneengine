import { useState } from 'react';
import ContentField from '@/components/studio/ContentField';
export default function ContentForm({item,fields,references,referenceSearch,busy,onSave,onDelete,onCancel,values,onChangeValues,saveLabel='Save content'}){
  const [error,setError]=useState('');
  const changeField=(field,value)=>{const next={...values,[field.key]:value};if(field.key==='interaction_actions'){const linked=(Array.isArray(value)?value:[]).filter(action=>action.type==='talk'&&action.dialogue_graph_id).map(action=>action.dialogue_graph_id);next.dialogue_graph_ids=[...new Set([...(values.dialogue_graph_ids||[]),...linked])]}onChangeValues(next)};
  const submit=e=>{e.preventDefault();setError('');onSave(values)};
  return <form onSubmit={submit} className="overflow-hidden rounded-xl border border-white/10 bg-[#0d1729]"><div className="border-b border-white/10 p-5"><div className="flex items-center gap-2"><span className="rounded bg-cyan-400 px-3 py-1.5 text-sm font-semibold text-slate-950">{item?.id?'Edit content':'New content'}</span><span className="rounded border border-cyan-400/20 px-3 py-1.5 text-sm text-cyan-300">Structured</span></div><h3 className="mt-4 text-lg font-semibold text-slate-100">Structure</h3><p className="text-sm text-slate-400">Structured fields and declarative JSON are validated before this content is saved.</p></div>{error&&<p className="mx-5 mt-4 text-sm text-red-300">{error}</p>}<div className="grid gap-5 p-5 xl:grid-cols-2">{fields.map(field=><ContentField key={field.key} field={field} value={values[field.key]} references={references} referenceSearch={referenceSearch} allValues={values} onChange={value=>changeField(field,value)}/>)}</div><div className="flex flex-wrap justify-end gap-2 border-t border-white/10 p-4">{item?.id&&onDelete&&<button type="button" disabled={busy} onClick={onDelete} className="mr-auto rounded border border-red-400/20 px-3 py-2 text-sm text-red-300">Delete</button>}{onCancel&&<button type="button" onClick={onCancel} className="rounded border border-white/10 px-3 py-2 text-sm">Cancel</button>}<button disabled={busy} className="rounded bg-runtime-accent px-4 py-2 text-sm font-semibold text-slate-950 disabled:opacity-50">{saveLabel}</button></div></form>;
}