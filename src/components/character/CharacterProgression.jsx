import { Link } from 'react-router-dom';
export default function CharacterProgression({skills,equipment,currency}){
  return <section className="rounded-lg border border-white/10 bg-runtime-surface"><div className="flex items-center justify-between gap-3 border-b border-white/10 p-4"><h3 className="font-semibold">Progression & equipment</h3><Link to="/skills" className="text-xs text-runtime-accent hover:underline">Manage skills & loadouts</Link></div><div className="grid gap-4 p-4 sm:grid-cols-3"><List title="Skills" rows={skills.map(skill=>[skill.definition?.name||skill.skill_definition_id,`Rank ${skill.rank}`])}/><List title="Equipped" rows={equipment.map(item=>[item.definition?.name||item.definition_id,item.equipped_slot])}/><List title="Currency" rows={Object.entries(currency||{}).map(([key,value])=>[key,value])}/></div></section>;
}
function List({title,rows}){return <div><h4 className="text-xs uppercase tracking-wider text-slate-500">{title}</h4><dl className="mt-2 space-y-2">{rows.map(([label,value])=><div key={`${label}-${value}`} className="flex justify-between gap-3 text-sm"><dt className="truncate capitalize">{label}</dt><dd className="shrink-0 text-slate-400 capitalize">{value}</dd></div>)}</dl>{!rows.length&&<p className="mt-2 text-sm text-slate-500">None</p>}</div>;}