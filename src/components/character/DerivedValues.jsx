export default function DerivedValues({values}) {
  if (!values?.length) return null;
  return <section className="rounded-lg border border-white/10 bg-runtime-surface"><div className="border-b border-white/10 p-4"><h3 className="font-semibold">Derived values</h3><p className="mt-1 text-xs text-slate-500">Calculated from effective attributes by versioned server formulas.</p></div><div className="grid gap-3 p-4 sm:grid-cols-2">{values.map(value=><div key={value.key} className="rounded border border-white/10 p-3"><div className="flex items-center justify-between gap-3"><span className="text-sm text-slate-400">{value.name}</span><strong className="font-mono text-lg text-runtime-accent">{value.value}</strong></div><p className="mt-1 text-xs text-slate-500">Formula result {value.formulaValue}{value.modifierTotal!==0&&` · Direct modifier ${value.modifierTotal>0?'+':''}${value.modifierTotal}`}</p>{value.sources.map((source,index)=><p key={`${source.source}-${index}`} className="text-xs text-slate-600">{source.source}: {source.amount>0?'+':''}{source.amount}</p>)}</div>)}</div></section>;
}