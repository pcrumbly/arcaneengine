export default function CharacterSummary({character}){
  const metrics=[['State',character.status||'active'],['Content version',character.content_version],['Record version',character.version]];
  return <section className="rounded-lg border border-white/10 bg-runtime-surface p-4"><div className="flex items-center gap-3"><div className="grid size-14 place-items-center rounded-lg bg-runtime-accent/15 text-lg font-bold text-runtime-accent">{character.name.slice(0,2).toUpperCase()}</div><div><h3 className="text-xl font-semibold">{character.name}</h3><p className="text-sm text-slate-500">Content-defined character profile</p></div></div><dl className="mt-4 grid gap-2 sm:grid-cols-3">{metrics.map(([label,value])=><div key={label} className="rounded border border-white/10 p-3"><dt className="text-[10px] uppercase tracking-wider text-slate-500">{label}</dt><dd className="mt-1 text-sm capitalize">{value}</dd></div>)}</dl>{!!character.tags?.length&&<div className="mt-3 flex flex-wrap gap-2">{character.tags.map(tag=><span key={tag} className="rounded bg-white/5 px-2 py-1 text-xs text-slate-300">{tag}</span>)}</div>}</section>;
}