function List({title,rows}){return <div><h4 className="text-xs uppercase tracking-wider text-slate-500">{title}</h4><dl className="mt-2 space-y-2">{rows.map(([label,value])=><div key={label} className="flex justify-between gap-3 text-sm"><dt className="truncate capitalize">{label}</dt><dd className="shrink-0 text-slate-400">{value}</dd></div>)}</dl>{!rows.length&&<p className="mt-2 text-sm text-slate-500">None</p>}</div>}
export default function CharacterSocialStanding({social}){const reputations=(social?.reputations||[]).flatMap(row=>Object.entries(row.values||{}).map(([key,value])=>[`${row.faction_key} · ${key}`,value])),legal=(social?.legal_standings||[]).map(row=>[row.jurisdiction_key,`${row.status} · ${row.severity}`]),knowledge=(social?.knowledge||[]).map(row=>[row.proposition_key,`${Math.round(Number(row.confidence||0)*100)}% confidence`]);return <section className="rounded-lg border border-white/10 bg-runtime-surface"><div className="border-b border-white/10 p-4"><h3 className="font-semibold">Social standing</h3></div><div className="grid gap-4 p-4 sm:grid-cols-3"><List title="Reputation" rows={reputations}/><List title="Legal standing" rows={legal}/><List title="Knowledge" rows={knowledge}/></div></section>}