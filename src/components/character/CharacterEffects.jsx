export default function CharacterEffects({effects}){
  return <section className="rounded-lg border border-white/10 bg-runtime-surface"><div className="border-b border-white/10 p-4"><h3 className="font-semibold">Active effects</h3></div><div className="divide-y divide-white/5">{effects.map(effect=><div key={effect.id} className="p-4"><div className="flex items-center justify-between gap-3"><strong className="text-sm">{effect.definition?.name||effect.status_key}</strong><span className="text-xs text-runtime-accent">{effect.stacks} stack{effect.stacks===1?'':'s'}</span></div>{effect.definition?.description&&<p className="mt-1 text-sm text-slate-400">{effect.definition.description}</p>}<p className="mt-1 text-xs text-slate-500">{effect.expires_at?`Expires ${new Date(effect.expires_at).toLocaleString()}`:effect.remaining_turns==null?'Persistent':`${effect.remaining_turns} turns remaining`}</p></div>)}{!effects.length&&<p className="p-4 text-sm text-slate-500">No active effects.</p>}</div></section>;
}