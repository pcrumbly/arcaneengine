import { Heart, Shield } from 'lucide-react';

export default function CombatRoster({ participants, selectedId, onSelect }) {
  return <div className="grid gap-3 sm:grid-cols-2">{['player','enemy'].map((team) => <section key={team} className="rounded-lg border border-white/10 bg-[#0a1728] p-3"><h3 className="mb-3 text-xs uppercase tracking-wider text-slate-500">{team === 'player' ? 'Party' : 'Opposition'}</h3><div className="space-y-2">{participants.filter((item) => item.team === team).map((item) => <button key={item.id} disabled={team === 'player' || item.status !== 'active'} onClick={() => onSelect(item.id)} className={`w-full rounded-md border p-3 text-left ${selectedId === item.id ? 'border-cyan-400/60 bg-cyan-400/5' : 'border-white/10 bg-white/[.025]'}`}><div className="flex justify-between"><strong className="text-sm">{item.name}</strong><span className="text-xs capitalize text-slate-500">{item.status}</span></div><div className="mt-2 flex items-center gap-4 text-xs text-slate-400"><span className="flex items-center gap-1"><Heart size={13}/>{item.resources?.vitality || 0}/{item.maximum_resources?.vitality || 100}</span><span className="flex items-center gap-1"><Shield size={13}/>{item.attributes?.defense || 0}</span></div></button>)}</div></section>)}</div>;
}