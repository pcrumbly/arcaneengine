import { LoaderCircle, Play } from 'lucide-react';

export default function EncounterList({encounters,pending,busy,onStart}){
  if(!encounters.length)return <div className="rounded-lg border border-dashed border-white/10 p-8 text-center"><h3 className="font-semibold">No encounters available</h3><p className="mt-2 text-sm text-slate-500">Move to another location or ask the game operator to configure an encounter here.</p></div>;
  return <div className="grid gap-3 sm:grid-cols-2">{encounters.map(encounter=><article key={encounter.id} className="rounded-lg border border-white/10 bg-runtime-surface p-5"><p className="text-xs uppercase tracking-wider text-runtime-accent">{(encounter.turn_model||'individual_initiative').replaceAll('_',' ')}</p><h3 className="mt-2 font-semibold">{encounter.name}</h3><p className="mt-2 text-sm leading-6 text-slate-400">{encounter.description||'No encounter briefing is available.'}</p><button disabled={busy} onClick={()=>onStart(encounter.id)} className="mt-4 flex items-center gap-2 rounded bg-runtime-accent px-3 py-2 text-sm font-semibold text-slate-950 disabled:opacity-50">{pending===`encounter:${encounter.id}`?<LoaderCircle size={15} className="animate-spin"/>:<Play size={15}/>} Start encounter</button></article>)}</div>;
}