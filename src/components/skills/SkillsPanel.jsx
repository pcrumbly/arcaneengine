export default function SkillsPanel({skills,currency,busy,onTrain}){return <section className="rounded-lg border border-white/10 bg-runtime-surface"><div className="border-b border-white/10 p-4"><h3 className="font-semibold">Skills</h3><p className="text-xs text-slate-500">Train content-defined capabilities using configured currency costs.</p></div><div className="divide-y divide-white/5">{skills.map(({definition,record})=>{const rank=record?.rank||0,maxed=rank>=definition.max_rank,cost=Object.entries(definition.training_costs||{}).map(([key,value])=>`${value} ${key}`).join(' · ');return <div key={definition.id} className="flex items-center justify-between gap-4 p-4"><div><div className="flex items-center gap-2"><strong>{definition.name}</strong><span className="text-xs text-runtime-accent">Rank {rank}/{definition.max_rank}</span></div><p className="mt-1 text-sm text-slate-400">{definition.description}</p><p className="mt-1 text-xs text-slate-500">{definition.category}{cost&&` · ${cost}`}</p></div><button disabled={busy||maxed} onClick={()=>onTrain(definition.id)} className="rounded border border-runtime-accent/30 px-3 py-2 text-sm text-runtime-accent disabled:opacity-30">{maxed?'Mastered':'Train'}</button></div>})}{!skills.length&&<p className="p-4 text-sm text-slate-500">No skills are defined for this content version.</p>}</div><p className="border-t border-white/10 p-3 text-xs text-slate-500">Available: {Object.entries(currency||{}).map(([key,value])=>`${value} ${key}`).join(' · ')||'No currency'}</p></section>}