import { RefreshCw } from 'lucide-react';
export default function SkillsToolbar({characters,characterId,onCharacterChange,onRefresh,busy}){return <div className="flex flex-col gap-2 sm:flex-row sm:justify-end"><select aria-label="Skills operator" value={characterId} disabled={busy} onChange={event=>onCharacterChange(event.target.value)} className="rounded border border-white/10 bg-runtime px-3 py-2 text-sm text-runtime-text disabled:opacity-50 sm:w-56">{characters.map(character=><option key={character.id} value={character.id}>{character.name}</option>)}</select><button type="button" disabled={busy} onClick={onRefresh} className="flex items-center justify-center gap-2 rounded border border-white/10 px-3 py-2 text-sm disabled:opacity-50"><RefreshCw size={15} className={busy?'animate-spin':''}/>Refresh</button></div>}