import { Plus } from 'lucide-react';
export default function LoadoutList({loadouts,selected,onSelect,onAdd}){return <aside className="rounded-lg border border-white/10 bg-runtime-surface"><div className="flex items-center justify-between border-b border-white/10 p-3"><div><h3 className="font-semibold">Loadouts</h3><p className="text-xs text-slate-500">{loadouts.length} saved</p></div><button onClick={onAdd} className="rounded bg-runtime-accent/10 p-2 text-runtime-accent" aria-label="Create loadout"><Plus size={16}/></button></div>{loadouts.map(loadout=><button key={loadout.id} onClick={()=>onSelect(loadout)} className={`block w-full border-b border-white/5 p-3 text-left ${selected?.id===loadout.id?'bg-runtime-accent/10':''}`}><strong className="text-sm">{loadout.name}</strong>{loadout.is_active&&<span className="ml-2 text-xs text-runtime-accent">Active</span>}<p className="mt-1 text-xs text-slate-500">{loadout.active_ability_ids?.length||0} abilities · {Object.keys(loadout.equipment_assignments||{}).length} equipped</p></button>)}{!loadouts.length&&<p className="p-4 text-sm text-slate-500">Create your first loadout.</p>}</aside>}