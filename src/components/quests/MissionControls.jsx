import { Search } from 'lucide-react';
const tabs=['active','available','completed','failed','abandoned','all'];
export default function MissionControls({tab,onTabChange,query,onQueryChange,characters,characterId,onCharacterChange,busy}){
 return <div className="flex flex-col gap-3 lg:flex-row lg:items-center lg:justify-between"><div role="tablist" aria-label="Mission views" className="flex gap-1 overflow-x-auto rounded-md border border-white/10 bg-runtime-surface p-1">{tabs.map(value=><button key={value} role="tab" aria-selected={tab===value} disabled={busy} onClick={()=>onTabChange(value)} className={`rounded px-3 py-1.5 text-xs capitalize disabled:opacity-50 ${tab===value?'bg-runtime-accent text-slate-950':'text-slate-400'}`}>{value}</button>)}</div><div className="flex flex-col gap-2 sm:flex-row"><label className="text-xs text-slate-400"><span className="sr-only">Operator</span><select aria-label="Mission operator" value={characterId} disabled={busy} onChange={event=>onCharacterChange(event.target.value)} className="w-full rounded-md border border-white/10 bg-runtime px-3 py-2 text-sm text-runtime-text sm:w-48">{characters.map(character=><option key={character.id} value={character.id}>{character.name}</option>)}</select></label><label className="relative"><span className="sr-only">Search missions</span><Search className="absolute left-3 top-2.5 text-slate-500" size={15}/><input value={query} onChange={event=>onQueryChange(event.target.value)} placeholder="Search missions" className="w-full rounded-md border border-white/10 bg-runtime py-2 pl-9 pr-3 text-sm sm:w-64"/></label></div></div>;
}