import { useEffect, useState } from 'react';
import { base44 } from '@/api/base44Client';
import PartySetup from '@/components/party/PartySetup';
import PartyRoster from '@/components/party/PartyRoster';

export default function Party(){const [data,setData]=useState(null),[busy,setBusy]=useState(false),[error,setError]=useState('');const invoke=async(payload)=>{setBusy(true);setError('');try{const {data:next}=await base44.functions.invoke('runtimeCommand',payload);setData(next)}catch(caught){setError(caught.response?.data?.error||caught.message)}finally{setBusy(false)}};useEffect(()=>{base44.functions.invoke('runtimeCommand',{command:'GET_STATE'}).then(({data:state})=>state.character&&invoke({command:'GET_PARTY',characterId:state.character.id}))},[]);if(!data)return <div className="p-6 text-sm text-slate-400">Loading party…</div>;const command=(payload)=>invoke({characterId:data.character.id,requestId:crypto.randomUUID(),...payload});return <div className="p-4 sm:p-6"><p className="text-xs uppercase tracking-[.22em] text-cyan-400">Runtime / Group management</p><h2 className="mt-1 text-2xl font-semibold">Party</h2><p className="mt-2 text-sm text-slate-400">Build a formation from your characters at the current location.</p>{error&&<div role="alert" className="mt-4 rounded-md border border-red-400/20 bg-red-400/10 p-3 text-sm text-red-200">{error}</div>}<div className="mt-6">{data.party?<PartyRoster {...data} busy={busy} onAdd={(memberCharacterId)=>command({command:'ADD_PARTY_MEMBER',memberCharacterId})} onRemove={(memberId)=>command({command:'REMOVE_PARTY_MEMBER',memberId})} onDisband={()=>command({command:'DISBAND_PARTY'})}/>:<PartySetup character={data.character} busy={busy} onCreate={(name)=>command({command:'CREATE_PARTY',name})}/>}</div></div>}